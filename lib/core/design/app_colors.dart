import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1B4DF5); // iOS blue
  static const accent = Color(0xFFF5BC1D); // iOS yellow/gold
  static const background = Color(0xFFF6F8FA); // subtle off-white
  static const surface = Color(0xCCFFFFFF); // glassy white with opacity
  static const glass = Color(0x80FFFFFF); // more transparent glass
  static const glassDark = Color(0xB31A1A1A); // dark glass for dark mode
  static const gradientStart = Color(0xFF4F8CFF);
  static const gradientEnd = Color(0xFFB6D0FF);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6B6B6B);
  static const success = Color(0xFF2F855A);
  static const warning = Color(0xFFF5BC1D);
  static const error = Color(0xFFE53935);
  static const shadow = Color(0x1A000000); // subtle shadow
}
